<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= restaurant.name %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= restaurant.name %></h1>
    <h3>Cuisine: <%= restaurant.cuisine %></h3>
    <p><%= restaurant.address %></p>
    <p><%= restaurant.phoneNumber %></p>
    <% if (restaurant.website) { %>
    <p>
      <a href="<%= restaurant.website %>" target="_blank"> Visit Website </a>
    </p>
    <% } %>
    <br />
    <br />
    <a href="/users/<%=user._id%>/restaurants/<%=restaurant._id%>/edit">Edit</a>
    <br />
    <br />
    <form action="/users/<%=user._id%>/restaurants/<%=restaurant._id%>?_method=DELETE" method="POST">
      <button type="submit">Delete</button>
    </form>
    <br />
  </body>
</html>
