<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= restaurant.name %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit <%= restaurant.name %> </h1>
    <form action="/users/<%=user._id%>/restaurants/<%= restaurant._id %>?_method=PUT" method="POST">
      <label for="city">Main City:</label>
      <select id="city" name="city" required>
        <option value="Houston" <%= restaurant.city === "Houston" ? "selected" : "" %> > Houston </option>
        <option value="Dallas" <%= restaurant.city === "Dallas" ? "selected" : "" %> > Dallas </option>
        <option value="Austin" <%= restaurant.city === "Austin" ? "selected" : "" %> > Austin </option>
        <option value="San Antonio" <%= restaurant.city === "San Antonio" ? "selected" : "" %> > San Antonio </option>
      </select>
      <br />
      <br />
      <label for="name">Name</label>
      <input type="text" id="name" name="name" value="<%= restaurant.name %>"required />
      <br />
      <br />
      <label for="cuisine">Cuisine</label>
      <input type="text" id="cuisine" name="cuisine" value="<%= restaurant.cuisine %>" required />
      <br />
      <br />
      <label for="address">Address</label>
      <input type="text" id="address" name="address" value="<%= restaurant.address %>"required />
      <br />
      <br />
      <label for="phoneNumber">Phone Number</label>
      <input type="text" id="phoneNumber" name="phoneNumber" value="<%= restaurant.phoneNumber %>"/>
      <br />
      <br />
      <label for="website">Website</label>
      <input type="text" id="website" name="website" value="<%= restaurant.website %>"/>
      <br />
      <br />
      <button type="submit">Submit</button>
      <button type="button" onclick="window.history.back()">Cancel</button>
    </form>
  </body>
</html>
